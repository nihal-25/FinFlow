{
  "installCommand": "npm install",
  "buildCommand": "npm run build:packages && npm run build -w frontend",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
