{
  "name": "@finflow/fraud-service",
  "version": "1.0.0",
  "description": "Real-time fraud detection using rule engine",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@finflow/database": "*",
    "@finflow/kafka": "*",
    "@finflow/redis": "*",
    "@finflow/types": "*",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
